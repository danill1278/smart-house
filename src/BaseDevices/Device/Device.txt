class Device {
    properties:
        #state: Boolean,
        #name: String,        
        #timer: Number
    methods:
        +constructor(name: string)
        +toString(): string
        +get state(): Boolean
        +get name(): String
        +set name(String): void
        +on(): voids
        +off(): void
        #isDeviceOn(): Boolean
        #deleteTimer(): void
}
