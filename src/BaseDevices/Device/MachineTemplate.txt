class Device {
    properties:
        #name: String,
        #state: Boolean
        #model: String
        #timer: Number
    methods:
        +constructor(name)
        +info(): string
        +get state(): Boolean
        +get name(): String
        +set name(String): void
        +get model(): String
        +on(): void
        +off(): void
        #isDeviceOn(): Boolean
        #checkNameValidity(): Boolean
        #deleteTimer(): void
}
